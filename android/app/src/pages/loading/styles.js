import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },

  logoContainer: {
    alignItems: 'center',
    flex: 1,
    justifyContent: 'center',
  },
  logoText: {fontSize: 25, fontWeight: 'bold', color: 'white'},
  logoSubText: {
    fontSize: 10,
    alignItems: 'center',
    fontWeight: 'bold',
    color: 'white',
  },
  gradient: {
    flex: 1,
  },
});

export default styles;
