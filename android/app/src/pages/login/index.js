import * as React from 'react';
import { Button } from 'react-native';
const Login = ({ navigation }) => {
    return (
      <Button
        title="Tela de login"

      />
    );
  };
  export default Login;